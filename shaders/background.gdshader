shader_type canvas_item;

uniform vec4 top_color : source_color = vec4(0.2, 0.3, 0.4, 1.0);
uniform vec4 bottom_color : source_color = vec4(0.1, 0.15, 0.2, 1.0);

void fragment() {
    COLOR = mix(top_color, bottom_color, UV.y);
}